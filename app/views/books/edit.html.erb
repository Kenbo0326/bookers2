<h1>Edit Book</h1>

<%= form_with model: @book, local: true do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <%= f.submit "Update Book" %>
<% end %>

<%= link_to "Back", books_path %>

